# Raw benchmark outputs are written here by benchmarks/run.sh (hey output:
# performance summary + latency distribution + status-code distribution).
# Not committed with real data; regenerate on the benchmark host.